---
category: index
bucket: lessons
generated: true
---

# Lessons Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one lesson entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Treat setup stats warnings as harness blockers](setup.md) (search: "## Lesson: Treat setup stats warnings as harness blockers") - When setup stats emits learning-loop warnings, resolve them before running the harness audit.
- [An Edit's new_string must re-include any trailing boundary its old_string captured as context](verification.md) (search: "## Lesson: An Edit's new_string must re-include any trailing boundary its old_string captured as context") - When an Edit's `old_string` reaches past the intended change to grab a trailing structural marker (a section header, a closing brace/bracket, a sentinel comment) only as insertion context, the…
- [Verify changed-region hunk tests against real finding spans](verification.md) (search: "## Lesson: Verify changed-region hunk tests against real finding spans") - When testing changed-region hunk behaviour, read the rule's `Finding` location shape first.
- [Anchor-only aggregate filtering must include decorated headers](verification.md) (search: "## Lesson: Anchor-only aggregate filtering must include decorated headers") - When changing anchor-only symbol filtering, test decorated declarations as well as undecorated ones.
- [New root commands must update the static root menu](verification.md) (search: "## Lesson: New root commands must update the static root menu") - When adding or renaming a root command, update both the Click registration and `src/gruffpy/cli_menu.py`, then run the full `uv run pytest tests/integration/test_cli_smoke.py` rather than only…
- [Read the rule body before calling a rule dead or leftover](verification.md) (search: "## Lesson: Read the rule body before calling a rule dead or leftover") - When a rule ID's lineage suggests it might not apply to this language, the ID is not evidence - read the `analyse` method and tests before stating "looks like a leftover" or "probably dead".
- [Run targeted formatting before broad verification on dirty surfaces](verification.md) (search: "## Lesson: Run targeted formatting before broad verification on dirty surfaces") - When a repo-wide non-mutating format check is already known to fail, run a touched-file format check or formatter pass before the broad gate so the agent-owned formatting state is separated from…
- [Re-run raw-source analyzers after formatting fixture strings](verification.md) (search: "## Lesson: Re-run raw-source analyzers after formatting fixture strings") - When a rule scans raw source text and tests need runtime strings containing trigger tokens, use a construction that the formatter will not fold back into the raw token, such as `"".join(("TO",…
- [Test directive parsers against reason delimiters before dogfood](verification.md) (search: "## Lesson: Test directive parsers against reason delimiters before dogfood") - When implementing source-comment directive parsers, test each supported directive with no reason, `-` / `--` reasons, second-`#` reasons, and bracketed payloads before broad dogfood.
- [Parser-confirmed comment heuristics need compound-header fixtures](verification.md) (search: "## Lesson: Parser-confirmed comment heuristics need compound-header fixtures") - When a source-text rule uses parser confirmation on snippets, include single-line compound headers (`if`, `for`, `while`, `try`, `with`, `def`, `class`) in counter-fixtures.
- [Scan scratch repos from their own project roots](verification.md) (search: "## Lesson: Scan scratch repos from their own project roots") - When comparing scan-test repos under a scratch directory, validate `filesParsed` and project-root findings before trusting timing or counts.
- [Rule removals must update dogfood config before full pytest](verification.md) (search: "## Lesson: Rule removals must update dogfood config before full pytest") - When adding or removing a built-in rule, grep and update `.gruff-py.yaml` alongside the catalog, generated docs, fixtures, and focused rule tests before running broad verification.
- [Inspect each item's body before bucketing tests for bulk deletion](verification.md) (search: "## Lesson: Inspect each item's body before bucketing tests for bulk deletion") - - 17 plain `assert d.id == "<literal>"` (truly trivial, redundant with `tests/unit/rule/test_catalog.py::test_no_concrete_rule_class_is_omitted_from_catalog`). - 15…
- [Reproduce Click option-parsing claims with CliRunner before reasoning about them](verification.md) (search: "## Lesson: Reproduce Click option-parsing claims with CliRunner before reasoning about them") - ``` argv=['--baseline', 'src'] -> "paths=() baseline_path='src'" argv=['--baseline', 'src', 'tests'] -> "paths=('tests',) baseline_path='src'" ```
- [Count registered rules from the runtime registry, not by grepping `_entry(`](verification.md) (search: "## Lesson: Count registered rules from the runtime registry, not by grepping `_entry(`") - When verifying catalog/registry counts, drive from the actual runtime state - `uv run python -c "from gruffpy.rule.registry import RuleRegistry; print(len(list(RuleRegistry.defaults().all())))"` -…
- [Static rule catalog scans must exclude support protocols](verification.md) (search: "## Lesson: Static rule catalog scans must exclude support protocols") - When converting dynamic rule discovery to source scanning, explicitly exclude support contract modules such as `project_rule.py` and keep the predicate tied to concrete rule implementation files, not…
- [Re-run the dogfood gate after adding branches, even when the change "feels small"](verification.md) (search: "## Lesson: Re-run the dogfood gate after adding branches, even when the change") - When extending any function that already had multiple `if` guards or `or`/`and` short-circuits, run `uv run gruff-py analyse <changed-file> --fail-on advisory` before claiming the change is…
- [Suppression directives need a `--` rationale suffix or docs.ignore-directive-reason fires](verification.md) (search: "## Lesson: Suppression directives need a `--` rationale suffix or docs.ignore-directive-reason fires") - When adding any `# gruff: disable...` directive, always append a short rationale after `--` on the same comment, e.g.
- [Quote YAML boolean-like strings in dogfood option defaults](verification.md) (search: "## Lesson: Quote YAML boolean-like strings in dogfood option defaults") - When adding string-list defaults to `.gruff-py.yaml`, quote YAML 1.1 boolean-like scalars such as `yes`, `no`, `on`, and `off`.
- [Split regression tests by review surface before dogfood](verification.md) (search: "## Lesson: Split regression tests by review surface before dogfood") - When a regression spans multiple outputs, keep one test per reviewer surface even if the setup is shared.
- [Tick task checkboxes when the proof passes, not during later cleanup](verification.md) (search: "## Lesson: Tick task checkboxes when the proof passes, not during later cleanup") - When a task file contains a checkbox for a command, evidence item, or exit criterion, update that checkbox immediately after the proof passes.
- [Keep self-check regression fixtures invisible to unrelated rules](verification.md) (search: "## Lesson: Keep self-check regression fixtures invisible to unrelated rules") - When editing regression fixtures for one rule, run the project dogfood check before final preflight and inspect cross-rule fixture shapes.
- [When tool output lags or duplicates, re-establish ground truth before editing — never edit against a stale read, never confabulate the contents of output you do not have](verification.md) (search: "## Lesson: When tool output lags or duplicates, re-establish ground truth before editing — never edit against a stale read, never confabulate the contents of output you do not have") - When tool results arrive late, out of order, or duplicated, stop issuing mutations and re-establish ground truth with checks that do not depend on a possibly-stale read: `git status --short`,…
- [Posture map tests must move with rule catalogue additions](verification.md) (search: "## Lesson: Posture map tests must move with rule catalogue additions") - When adding or deleting a default-enabled rule, update both catalogue/count surfaces and any posture map that asserts whole rule families.
- [Reproduce rule false-positive claims by running `gruff-py analyse` on a crafted fixture](verification.md) (search: "## Lesson: Reproduce rule false-positive claims by running `gruff-py analyse` on a crafted fixture") - When a review (bot or human) claims a rule fires, misses, or false-positives, reproduce it by running `gruff-py analyse` on a minimal crafted fixture and reading the findings for that rule id before…
- [Module-scope invalidation walks must cover nested statement blocks](verification.md) (search: "## Lesson: Module-scope invalidation walks must cover nested statement blocks") - When an allowlist depends on "single assignment at module scope", iterating `tree.body` alone is not single-assignment proof: rebinds hide in nested module-level blocks, loop targets, tuple…
- [`except tokenize.TokenError` is not enough, and one site is never all sites](verification.md) (search: "## Lesson: `except tokenize.TokenError` is not enough, and one site is never all sites") - When fixing an exception-handling gap, grep for every other call site of the same API (`generate_tokens`) before declaring the fix done, and verify with an end-to-end repro (real CLI on a broken…
- [Mirror Load-context guards across every reference-recording visitor path](verification.md) (search: "## Lesson: Mirror Load-context guards across every reference-recording visitor path") - When a `NodeVisitor` counts references, every recording path (`visit_Name`, `visit_Attribute`, any future handler) must apply the same `expr_context` discipline.
- [Always run `git status` before suggesting a commit message](workflow.md) (search: "## Lesson: Always run `git status` before suggesting a commit message") - Before suggesting any commit message, run `git status` (and `git diff --cached` when something is staged, or `git diff` when nothing is staged) to see what is actually about to be committed.
- [Read `RuleDocs` (and the rule catalogue) before scoping any new rule-metadata feature](workflow.md) (search: "## Lesson: Read `RuleDocs` (and the rule catalogue) before scoping any new rule-metadata feature") - Before scoping any feature framed as "extend the rule system with X", do a focused read of `src/gruffpy/rule/catalog.py` (the `RuleDocs` dataclass and the `_docs_for_definition` / `_custom_docs_for`…
- [Never offer to commit - "offering" still violates the Never tier](workflow.md) (search: "## Lesson: Never offer to commit -") - The rationalisation pattern was: "offering to commit isn't the same as committing; I am giving the user an option".
- [Treat CLAUDE.md Never-tier items as absolute, not preferences to weigh](workflow.md) (search: "## Lesson: Treat CLAUDE.md Never-tier items as absolute, not preferences to weigh") - CLAUDE.md's autonomy tiers split into Always (mandatory), Ask First (boundary-flagged), and Never (forbidden).
- [Do not bundle destructive cleanup with productive work in one command](workflow.md) (search: "## Lesson: Do not bundle destructive cleanup with productive work in one command") - When a guard hook can reject a command, never combine a destructive op (`rm -r`, etc.) with productive work in the same compound command: a rejection drops the entire command, so the productive part…

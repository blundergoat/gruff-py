---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [run_analysis(baseline=None) auto-applies the default baseline file](baseline.md) (search: "## Footgun: run_analysis(baseline=None) auto-applies the default baseline file") - `run_analysis` defaults `baseline` to `None`, which the runner substitutes with `BaselineOptions()`…
- [Click optional-value options + variadic positionals stay tempting](cli.md) (search: "## Footgun: Click optional-value options + variadic positionals stay tempting") - The underlying Click pattern - declaring an option with `is_flag=False, flag_value="..."` so a bare…
- [cli.py routinely sits within 20 lines of the file-length error threshold](cli.md) (search: "## Footgun: cli.py routinely sits within 20 lines of the file-length error threshold") - `src/gruffpy/cli.py` (search: `class CliGroup`) is the project's Click entry point and…
- [Finding fingerprints depend on PHP-style JSON bytes](compatibility.md) (search: "## Footgun: Finding fingerprints depend on PHP-style JSON bytes") - The fingerprint algorithm in `src/gruffpy/finding/fingerprint.py` is deliberately not plain…
- [hook and analysis `stableIdentity` use different input sets for symbol-less file/project findings](compatibility.md) (search: "## Footgun: hook and analysis `stableIdentity` use different input sets for symbol-less file/project findings") - Two identity schemes share the field name `stableIdentity` but diverge.
- [Frozen AnalysisConfig is not deeply immutable](compatibility.md) (search: "## Footgun: Frozen AnalysisConfig is not deeply immutable") - `AnalysisConfig` is a frozen dataclass, but its `rules` field is a mutable dictionary.
- [Compatibility CLI flags can be help-only](compatibility.md) (search: "## Footgun: Compatibility CLI flags can be help-only") - Some CLI flags exist for sibling-port surface compatibility before their runtime plumbing exists.
- [Removing a registered rule ID breaks downstream config loads](compatibility.md) (search: "## Footgun: Removing a registered rule ID breaks downstream config loads") - `ConfigLoader` rejects any `rules.<id>` entry whose key is not present in `RuleRegistry.defaults()`.
- [minimumSeverity defaults span six surfaces in lockstep](compatibility.md) (search: "## Footgun: minimumSeverity defaults span six surfaces in lockstep") - When the family-wide `--fail-on` binary default for a gateable subcommand moves, six source…
- [ConfigLoader `.get(key, [])` collapses absent into empty, clobbering seeded AnalysisConfig defaults](config.md) (search: "## Footgun: ConfigLoader `.get(key, [])` collapses absent into empty, clobbering seeded AnalysisConfig defaults") - Any new non-empty default added to an `AnalysisConfig` field that is also user-configurable under…
- [`architecture.md` / `README.md` rule counts and default-off claims drift from the live catalogue](docs.md) (search: "## Footgun: `architecture.md` / `README.md` rule counts and default-off claims drift from the live catalogue") - Hand-written rule-count summaries and capability claims in the docs go stale because the catalogue…
- [Docs parameter rules treat leading cls as an implicit receiver](docs.md) (search: "## Footgun: Docs parameter rules treat leading cls as an implicit receiver") - `src/gruffpy/rule/docs/_helpers.py` (`signature_param_names`) intentionally removes a leading…
- [Committed docs cite milestone/task codes that point into the gitignored tasks dir](docs.md) (search: "## Footgun: Committed docs cite milestone/task codes that point into the gitignored tasks dir") - Tracked docs across the repo cite milestone codes (`M01`, `M22`, `M33+M34+M35`, etc.) as if they…
- [goat-flow hook sync removes the project-local Codex quality hook](hooks.md) (search: "## Footgun: goat-flow hook sync removes the project-local Codex quality hook") - The project registers its changed-line quality check directly in `.codex/hooks.json` (search:…
- [Hatchling sdist can include ignored local workspace artifacts](packaging.md) (search: "## Footgun: Hatchling sdist can include ignored local workspace artifacts") - Hatchling's source distribution selection can include files that are local-only or ignored by…
- [Shell clean steps must tolerate missing build output directories](packaging.md) (search: "## Footgun: Shell clean steps must tolerate missing build output directories") - `find <dir> -delete` exits non-zero when `<dir>` does not exist.
- [publish gates must not assume publish-before-tag release order](packaging.md) (search: "## Footgun: publish gates must not assume publish-before-tag release order") - The release workflow tags before publishing.
- [`RuleDefinition.description` is a short label, not sentence-level prose](rules.md) (search: "## Footgun: `RuleDefinition.description` is a short label, not sentence-level prose") - `RuleDefinition.description` (`src/gruffpy/rule/definition.py`, search: `description: str = ""`)…
- [per-function rules that bare-`ast.walk` a function descend into nested scopes](rules.md) (search: "## Footgun: per-function rules that bare-`ast.walk` a function descend into nested scopes") - A per-function rule that calls `ast.walk(function)` to collect call sites, assignments, or guards…
- [`dead-code.exported-but-unreferenced` flags `__all__` exports by design - the library noise is not a bug](rules.md) (search: "## Footgun: `dead-code.exported-but-unreferenced` flags `__all__` exports by design - the library noise is not a bug") - `dead-code.exported-but-unreferenced`…
- [rules that read `x in y` as substring containment false-positive on collection-typed `y`](rules.md) (search: "## Footgun: rules that read `x in y` as substring containment false-positive on collection-typed `y`") - `in` is overloaded: `term in text` is substring containment for a `str`, but membership for a…
- [parent-walk exemptions must confirm the node is in the `if`/`try` *body*, not its `else`/`finally`](rules.md) (search: "## Footgun: parent-walk exemptions must confirm the node is in the `if`/`try` *body*, not its `else`/`finally`") - A rule that exempts or protects a node by walking up to an enclosing guard (an `if __name__ ==…
- [exemption / safe-guard matchers recognize only the canonical spelling and miss equivalent variants](rules.md) (search: "## Footgun: exemption / safe-guard matchers recognize only the canonical spelling and miss equivalent variants") - When a rule keys on a syntactic shape to *exempt* a node or treat a guard as "safe", it tends to…
- [fixed sentinels collide with decoded user literals](rules.md) (search: "## Footgun: fixed sentinels collide with decoded user literals") - Rules that flatten an AST template into text can accidentally use a character the parser also…
- [deleting a rule `.py` file leaves stale `__pycache__/*.pyc` that may keep the rule "registered"](setup.md) (search: "## Footgun: deleting a rule `.py` file leaves stale `__pycache__/*.pyc` that may keep the rule") - hallucination-risk: high (the change you made to `catalog.py` looks correct, the test suite passes…
- [uv sync can leave editable console scripts stale or missing](setup.md) (search: "## Footgun: uv sync can leave editable console scripts stale or missing") - After dependency changes, `uv sync --all-extras --dev --locked` can consider the editable…
- [Gitignore ignored-parent guard cannot be removed for re-includes](source.md) (search: "## Footgun: Gitignore ignored-parent guard cannot be removed for re-includes") - `GitignoreMatcher` needs both the ignored-parent early return and the contents-glob directory…

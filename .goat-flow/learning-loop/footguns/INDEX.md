---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [run_analysis(baseline=None) auto-applies the default baseline file](baseline.md) (search: "## Footgun: run_analysis(baseline=None) auto-applies the default baseline file") - `run_analysis` defaults `baseline` to `None`, which the runner substitutes with `BaselineOptions()` (apply_path=None, generate_path=None, disabled=False).
- [Click optional-value options + variadic positionals stay tempting](cli.md) (search: "## Footgun: Click optional-value options + variadic positionals stay tempting") - The underlying Click pattern - declaring an option with `is_flag=False, flag_value="..."` so a bare `--option` falls back to a default value - is unambiguous on its own, but combines disastrously…
- [cli.py routinely sits within 20 lines of the file-length error threshold](cli.md) (search: "## Footgun: cli.py routinely sits within 20 lines of the file-length error threshold") - `src/gruffpy/cli.py` (search: `class CliGroup`) is the project's Click entry point and concentrates: the root command group, every subcommand handler (`analyse`, `dashboard`, `init`, `list-rules`,…
- [Finding fingerprints depend on PHP-style JSON bytes](compatibility.md) (search: "## Footgun: Finding fingerprints depend on PHP-style JSON bytes") - The fingerprint algorithm in `src/gruffpy/finding/fingerprint.py` is deliberately not plain `json.dumps(...).sha256()`.
- [hook and analysis `stableIdentity` use different input sets for symbol-less file/project findings](compatibility.md) (search: "## Footgun: hook and analysis `stableIdentity` use different input sets for symbol-less file/project findings") - Two identity schemes share the field name `stableIdentity` but diverge.
- [Frozen AnalysisConfig is not deeply immutable](compatibility.md) (search: "## Footgun: Frozen AnalysisConfig is not deeply immutable") - `AnalysisConfig` is a frozen dataclass, but its `rules` field is a mutable dictionary.
- [Compatibility CLI flags can be help-only](compatibility.md) (search: "## Footgun: Compatibility CLI flags can be help-only") - Some CLI flags exist for sibling-port surface compatibility before their runtime plumbing exists.
- [Removing a registered rule ID breaks downstream config loads](compatibility.md) (search: "## Footgun: Removing a registered rule ID breaks downstream config loads") - `ConfigLoader` rejects any `rules.<id>` entry whose key is not present in `RuleRegistry.defaults()`.
- [minimumSeverity defaults span six surfaces in lockstep](compatibility.md) (search: "## Footgun: minimumSeverity defaults span six surfaces in lockstep") - When the family-wide `--fail-on` binary default for a gateable subcommand moves, six source surfaces must move together: the deciding ADR…
- [ConfigLoader `.get(key, [])` collapses absent into empty, clobbering seeded AnalysisConfig defaults](config.md) (search: "## Footgun: ConfigLoader `.get(key, [])` collapses absent into empty, clobbering seeded AnalysisConfig defaults") - Any new non-empty default added to an `AnalysisConfig` field that is also user-configurable under `[tool.gruff-py.allowlists]` (or another similarly-applied section) can be silently zeroed out by…
- [`architecture.md` / `README.md` rule counts and default-off claims drift from the live catalogue](docs.md) (search: "## Footgun: `architecture.md` / `README.md` rule counts and default-off claims drift from the live catalogue") - Hand-written rule-count summaries and capability claims in the docs go stale because the catalogue is the source of truth, not the prose.
- [Docs parameter rules treat leading cls as an implicit receiver](docs.md) (search: "## Footgun: Docs parameter rules treat leading cls as an implicit receiver") - `src/gruffpy/rule/docs/_helpers.py` (`signature_param_names`) intentionally removes a leading `self` or `cls` parameter before docstring matching.
- [Committed docs cite milestone/task codes that point into the gitignored tasks dir](docs.md) (search: "## Footgun: Committed docs cite milestone/task codes that point into the gitignored tasks dir") - Tracked docs across the repo cite milestone codes (`M01`, `M22`, `M33+M34+M35`, etc.) as if they were stable references, but the task files those codes name live under `.goat-flow/tasks/`, which is…
- [Hatchling sdist can include ignored local workspace artifacts](packaging.md) (search: "## Footgun: Hatchling sdist can include ignored local workspace artifacts") - Hatchling's source distribution selection can include files that are local-only or ignored by nested project workspace rules unless the sdist target excludes them explicitly.
- [Shell clean steps must tolerate missing build output directories](packaging.md) (search: "## Footgun: Shell clean steps must tolerate missing build output directories") - `find <dir> -delete` exits non-zero when `<dir>` does not exist.
- [publish gates must not assume publish-before-tag release order](packaging.md) (search: "## Footgun: publish gates must not assume publish-before-tag release order") - The release workflow tags before publishing.
- [`RuleDefinition.description` is a short label, not sentence-level prose](rules.md) (search: "## Footgun: `RuleDefinition.description` is a short label, not sentence-level prose") - `RuleDefinition.description` (`src/gruffpy/rule/definition.py`, search: `description: str = ""`) carries a 9-38 character label across the 115-rule catalogue (mean 21 chars).
- [per-function rules that bare-`ast.walk` a function descend into nested scopes](rules.md) (search: "## Footgun: per-function rules that bare-`ast.walk` a function descend into nested scopes") - A per-function rule that calls `ast.walk(function)` to collect call sites, assignments, or guards visits nested `def` / `class` / `lambda` bodies too.
- [`dead-code.exported-but-unreferenced` flags `__all__` exports by design - the library noise is not a bug](rules.md) (search: "## Footgun: `dead-code.exported-but-unreferenced` flags `__all__` exports by design - the library noise is not a bug") - `dead-code.exported-but-unreferenced` (`src/gruffpy/rule/dead_code/exported_but_unreferenced_rule.py`) deliberately counts `__all__` membership and bare re-export imports as NON-use: a public symbol…
- [rules that read `x in y` as substring containment false-positive on collection-typed `y`](rules.md) (search: "## Footgun: rules that read `x in y` as substring containment false-positive on collection-typed `y`") - `in` is overloaded: `term in text` is substring containment for a `str`, but membership for a `dict` / `set` / `list` / `Mapping`.
- [parent-walk exemptions must confirm the node is in the `if`/`try` *body*, not its `else`/`finally`](rules.md) (search: "## Footgun: parent-walk exemptions must confirm the node is in the `if`/`try` *body*, not its `else`/`finally`") - A rule that exempts or protects a node by walking up to an enclosing guard (an `if __name__ == "__main__":`, or a `try` with a matching handler) must verify the node sits in that block's `body`, not…
- [exemption / safe-guard matchers recognize only the canonical spelling and miss equivalent variants](rules.md) (search: "## Footgun: exemption / safe-guard matchers recognize only the canonical spelling and miss equivalent variants") - When a rule keys on a syntactic shape to *exempt* a node or treat a guard as "safe", it tends to match only the simplest spelling and miss equivalent ones - producing false positives (an unrecognized…
- [deleting a rule `.py` file leaves stale `__pycache__/*.pyc` that may keep the rule "registered"](setup.md) (search: "## Footgun: deleting a rule `.py` file leaves stale `__pycache__/*.pyc` that may keep the rule") - hallucination-risk: high (the change you made to `catalog.py` looks correct, the test suite passes for the deleted-rule path, and yet a freshly-run `gruff-py init` keeps emitting the deleted rule's…
- [uv sync can leave editable console scripts stale or missing](setup.md) (search: "## Footgun: uv sync can leave editable console scripts stale or missing") - After dependency changes, `uv sync --all-extras --dev --locked` can consider the editable `gruff-py` package already installed while the `gruff-py` console script is missing from `.venv/bin`.
- [Gitignore ignored-parent guard cannot be removed for re-includes](source.md) (search: "## Footgun: Gitignore ignored-parent guard cannot be removed for re-includes") - `GitignoreMatcher` needs both the ignored-parent early return and the contents-glob directory special case.

---
category: index
bucket: patterns
generated: true
---

# Patterns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one pattern entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Adding a top-level cross-impl config key](configuration.md) (search: "## Pattern: Adding a top-level cross-impl config key") - When a new top-level config key needs to ship in lockstep across sibling gruff implementations (e.g. the family-wide `minimumSeverity:` block landed in 0.1.2 per ADR-019), the key has to land cleanly…
- [Split scaffold-via-`yaml.safe_dump` and rules-via-manual-string when init output needs per-rule comments](configuration.md) (search: "## Pattern: Split scaffold-via-`yaml.safe_dump` and rules-via-manual-string when init output needs per-rule comments") - `render_default_config_yaml` originally built one Python dict (paths + allowlists + selection + rules) and round-tripped it through `yaml.safe_dump(document, sort_keys=False,…
- [Keep generated config defaults synchronized across renderer, tests, and docs](configuration.md) (search: "## Pattern: Keep generated config defaults synchronized across renderer, tests, and docs") - `gruff-py init` emits a concrete starter `.gruff-py.yaml`, not just the in-memory `AnalysisConfig.from_registry()` defaults.
- [Raise a typed exception at the validation layer, convert to `click.ClickException` at the CLI boundary](error-handling.md) (search: "## Pattern: Raise a typed exception at the validation layer, convert to `click.ClickException` at the CLI boundary") - A loader, parser, or other deep-stack helper detects a user-actionable problem (malformed config, missing required field, conflicting CLI flags expressed as data, etc.) and needs to surface a clean,…
- [Measure performance changes with the shipped harness](performance.md) (search: "## Pattern: Measure performance changes with the shipped harness") - Performance changes to rule traversal and source discovery can look locally obvious but still move costs between workloads.
- [Gate expensive rule walks with necessary source tokens](performance.md) (search: "## Pattern: Gate expensive rule walks with necessary source tokens") - Rule modules that inspect rare sink shapes can spend most of their cost walking ASTs for files that cannot possibly match.
- [Prove rule-precision relaxations with paired scratch repros](rules.md) (search: "## Pattern: Prove rule-precision relaxations with paired scratch repros") - Precision fixes for security rules can remove noisy findings while quietly changing serialized labels or dropping attack shapes.
- [Frame rule messages by what the user should add, not what's absent](rules.md) (search: "## Pattern: Frame rule messages by what the user should add, not what's absent") - Engineering agents trained on "no boilerplate" instructions read messages like `"Function 'foo' has no docstring."` as a request for restate-the-signature filler and decline to act on principle.
- [Add rules end to end](rules.md) (search: "## Pattern: Add rules end to end") - New rules need to participate in config defaults, selection, execution, reporting, scoring, and tests.
- [Split severity thresholds from named tuning knobs](rules.md) (search: "## Pattern: Split severity thresholds from named tuning knobs") - gruff-php and gruff-py support two different numeric config shapes.
- [Regression-test same-shape expression false positives](rules.md) (search: "## Pattern: Regression-test same-shape expression false positives") - Analyzer helpers that compare AST expressions can accidentally treat same-shaped calls on different receivers as identical.
- [Use `ast.Call` presence to distinguish Acts from data unpacking](rules.md) (search: "## Pattern: Use `ast.Call` presence to distinguish Acts from data unpacking") - Test-quality heuristics often need to discriminate between a "real operation" (Act) and "data unpacking" (subscript / attribute access / literal or dict-comp restructuring) between assertions.
